{
    "name": "@medminder/shared",
    "version": "1.0.0",
    "private": true,
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rimraf dist",
        "type-check": "tsc --noEmit",
        "lint": "eslint src/"
    },
    "dependencies": {
        "zod": "^3.24.0"
    },
    "devDependencies": {
        "typescript": "^5.7.0",
        "rimraf": "^6.0.0"
    }
}